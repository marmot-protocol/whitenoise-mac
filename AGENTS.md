# Agent Notes

- When validating the macOS app, keep exactly one `White Noise` instance running. Do not use `open -n`; before relaunching, quit or terminate the existing `White Noise` process, then launch a single replacement through Xcode or the built app.
