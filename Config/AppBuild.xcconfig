// App target build settings shared by Debug and Release.
// This file is for non-secret build-time values only. Runtime bearer tokens
// must be supplied through the launch environment; do not bundle them in the app.

#include? "AppSecrets.xcconfig"
